<view class="container">
  <image mode="aspectFill" class="head-image" src="{{movie.images.large}}" />
  <view class="head-img-hover">
    <text class="main-title">{{movie.title}}</text>
    <text class="sub-title">{{movie.countries[0]+'·'+movie.year}}</text>
    <view class="like">
      <text class="highlight-font">{{movie.wish_count}}</text>
      <text class="plain-font">人喜欢</text>
      <text class="highlight-font">{{movie.comments_count}}</text>
      <text class="plain-font">条评分</text>
    </view>
    <image bind:tap="onViewPost" class="movie-img" src="{{movie.images.large}}" />
  </view>
</view>